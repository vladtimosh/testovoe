export class Author {
    authorId: number;
    name: string;
    birthDate: Date;
  
    constructor(authorId: number, name: string, birthDate: Date) {
      this.authorId = authorId;
      this.name = name;
      this.birthDate = birthDate;
    }
  
  }
  